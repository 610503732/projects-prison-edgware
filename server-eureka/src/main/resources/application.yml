server:
  port: 7001
spring:
  application:
    name: eureka
eureka:
  client:
    service-url:
      defaultZone: http://localhost:7001/eureka/
    fetch-registry: false
    register-with-eureka: false
#注册中心安全机制
security:
  user:
    name: user
    password: 123456